tasks:
  robot:
    shell: python rake.py robot

devTasks:
  unitTests:
    shell: python rake.py test
  build:
    shell: python rake.py build
  local:
    shell: python rake.py local
  tools:
    shell: python rake.py tooling
  toc:
    shell: python rake.py toc

environmentConfigs:
- setup.yaml

artifactsDir: tmp

PATH:
PYTHONPATH:
ignoreFiles:
- .gitignore
